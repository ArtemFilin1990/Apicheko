[project]
name = "dadata-enrichment"
version = "0.1.0"
description = "DaData MCP server for company enrichment"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.21.0",
]

[project.scripts]
dadata-enrichment = "dadata_server:main"
